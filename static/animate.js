//function to linearly animate an existing HTML element (the object) moving from its current location to another
//animations done in the global coordinate system - the object is first moved to be a child of the game_div

function animate(object, endpoint, duration, finish_function){
	//object: the existing HTML element to animate
	//endpoint: {x: pixel_coord, y: pixel_coord}
	//duration: in ms
	//finish_function: function to call upon completion of animation. If left undefined, no function will be called
	
	
	//make object a child of game_div, preserving its location on screen
	
}